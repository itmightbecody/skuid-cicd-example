language: node_js
node_js: 
  - 10
services:
  - docker
before_script:
  - docker pull travisci/ci-nodejs
  - docker run -d -p 127.0.0.1:80:4567 travisci/ci-nodejs /bin/sh
  - docker ps -a
  - docker run travisci/ci-nodejs /bin/sh
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
addons:
  chrome: stable
jobs:
  include:
    - stage: test
    - stage: deploy
      if: branch = master
      script: bash scripts/deploy.sh